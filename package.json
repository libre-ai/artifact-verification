{
  "name": "@libre-ai/artifacts-gates",
  "private": true,
  "license": "EUPL-1.2",
  "type": "module",
  "packageManager": "bun@1.4.0-canary.1",
  "engines": {
    "bun": ">=1.4.0"
  },
  "overrides": {
    "fast-uri": "4.1.4"
  },
  "devDependencies": {
    "@libre-ai/governance": "file:../project-governance",
    "@biomejs/biome": "2.5.3",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  },
  "scripts": {
    "check:bun:runtime": "bun node_modules/@libre-ai/governance/tools/quality/check-bun-minimum.ts",
    "check:bun": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-bun-manifests.ts",
    "check:toolchain": "bun run check:bun && bun node_modules/@libre-ai/governance/tools/quality/check-toolchain.ts",
    "check:secret-scan": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-secret-scan.ts",
    "check:personal-data": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-personal-data-boundary.ts",
    "lint": "bun run check:bun:runtime && biome ci package.json biome.json",
    "test": "cargo test --locked && bun run --cwd packages/provenance test",
    "check": "bun run check:bun && bun run check:toolchain && bun run check:secret-scan && bun run check:personal-data && bun run lint && bun run check:rust && bun run check:provenance && bun run check:coverage",
    "pretest": "bun run check:bun",
    "check:rust": "bun run check:bun:runtime && cargo fmt --all --check && cargo clippy --locked --all-targets -- -D warnings && cargo test --locked",
    "check:provenance": "bun run check:bun:runtime && bun run --cwd packages/provenance check",
    "check:coverage": "bun run check:bun:runtime && cargo coverage-check"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/libre-ai/artifact-verification.git"
  },
  "workspaces": [
    "packages/provenance"
  ]
}
